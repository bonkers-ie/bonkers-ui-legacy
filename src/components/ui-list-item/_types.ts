export enum EListItemTypes {
	DEFAULT = "DEFAULT",
	PROGRESS = "PROGRESS"
}
export enum EListItemSpacing {
	DEFAULT = "DEFAULT",
	COMPACT = "COMPACT"
}

export enum EListItemSize {
	SM = "SM",
	MD = "MD"
}
