import { withInstall } from "../../_types/helper";
import RadioCompact from "./ui-radio-compact.vue";

/**
 * @deprecated The default should not be used - use named import as - import {[name]} from "ui/[name]"
 */
export const UiRadioCompact = withInstall(RadioCompact);
export default UiRadioCompact;
